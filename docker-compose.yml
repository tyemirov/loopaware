services:
  loopaware:
    build: .
    container_name: loopaware
    env_file:
      - .env.sample
    volumes:
      - loopaware-data:/app/data
    ports:
      - "8080:8080"
    restart: unless-stopped

volumes:
  loopaware-data:
